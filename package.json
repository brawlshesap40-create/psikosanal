{
  "name": "psikosanal",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:generate": "dotenv -e .env.local -- drizzle-kit generate --config packages/db/drizzle.config.ts",
    "db:migrate": "dotenv -e .env.local -- drizzle-kit migrate --config packages/db/drizzle.config.ts",
    "db:studio": "dotenv -e .env.local -- drizzle-kit studio --config packages/db/drizzle.config.ts",
    "db:seed": "dotenv -e .env.local -- tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@psikosanal/db": "*",
    "bcryptjs": "^3.0.3",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "tsx": "^4.23.12",
    "typescript": "^5"
  }
}
